import 'dart:ui';

class PFc {
  // **********************************************************************************
  // 主题色
  static const Color themeColor = Color.fromRGBO(77, 196, 96, 1);

  // 主题色20%透明度
  static const Color themeColor10 = Color.fromRGBO(77, 196, 96, 0.1);

  // 主题色20%透明度
  static const Color themeColor20 = Color.fromRGBO(77, 196, 96, 0.2);

  // 主题色30%透明度
  static const Color themeColor30 = Color.fromRGBO(77, 196, 96, 0.3);

  // **********************************************************************************

  // 主背景
  static const Color backgroundPrimary = Color.fromRGBO(246, 246, 246, 1);

  // 次背景
  static const Color backgroundSecondary = Color.fromRGBO(255, 255, 255, 1);

  // 灰背景
  static const Color backgroundBlack10 = Color.fromRGBO(67, 67, 67, 0.1);

  // 灰黑背景
  static const Color backgroundBlack = Color.fromRGBO(67, 67, 67, 1);

  // **********************************************************************************

  // 主文本 黑灰色
  static const Color textPrimary = Color.fromRGBO(51, 51, 51, 1);

  // 次文本 灰色
  static const Color textSecondary = Color.fromRGBO(136, 136, 136, 1);

  // 强调文本 绿色
  static const Color textEmphasis = Color.fromRGBO(77, 196, 96, 1);

  // 白文本
  static const Color textWhite = Color.fromARGB(255, 255, 255, 255);

  // **********************************************************************************

  // 主控件-背景 蓝色
  static const Color primaryElement = Color.fromRGBO(41, 103, 255, 1);

  // 主控件-文本 白色
  static const Color primaryElementText = Color.fromRGBO(255, 255, 255, 1);

// **********************************************************************************

  // 第二种控件-背景色 淡灰色
  static const Color secondaryElement = Color.fromRGBO(246, 246, 246, 1);

  // 第二种控件-文本 浅蓝色
  static const Color secondaryElementText = Color.fromRGBO(41, 103, 255, 1);

// **********************************************************************************
  // 第三种控件-背景色 石墨色
  static const Color thirdElement = Color.fromRGBO(45, 45, 47, 1);

  // 第三种控件-文本 浅灰色2
  static const Color thirdElementText = Color.fromRGBO(141, 141, 142, 1);

// **********************************************************************************

  // tabBar 默认颜色 灰色
  static const Color tabBarElement = Color.fromRGBO(208, 208, 208, 1);

  // tabCellSeparator 单元格底部分隔条 颜色
  static const Color tabCellSeparator = Color.fromRGBO(230, 230, 231, 1);
}
